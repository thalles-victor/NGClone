import { createStitches } from "@stitches/react";

export const {
  styled,
  keyframes,
  config,
  createTheme,
  theme,
  globalCss,
  getCssText,
} = createStitches({
  theme: {
    colors: {
      orange: "#FB5705",
      "text-color": "#5311FA",
    },
  },
});
